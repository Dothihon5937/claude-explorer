# Claude Explorer Configuration

# Optional: Port for web server (default: 3000)
PORT=3000

# Note: Authentication is now handled via OAuth (like Claude Code)
# Run 'npm run login' to authenticate with your claude.ai account
# Credentials are stored securely in ~/.claude-explorer/auth.json
